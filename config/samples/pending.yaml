apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
  labels:
    job-name: my-fake-job
spec:
  containers:
  - name: oversized
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    resources:
      requests:
        memory: "500Gi"
        cpu: "1000"
  restartPolicy: Never
