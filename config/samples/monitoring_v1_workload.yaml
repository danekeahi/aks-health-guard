apiVersion: monitoring.healthcheck.dev/v1
kind: Workload
metadata:
  labels:
    app.kubernetes.io/name: aks-health-guard-clean
    app.kubernetes.io/managed-by: kustomize
  name: workload-sample
spec:
  # TODO(user): Add fields here
  jobName: "my-fake-job"
  podCrashThreshold: 3
  maxRestartCount: 5
  maxPendingDuration: 60000000000
status: 
  health: true
